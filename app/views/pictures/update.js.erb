<% if @status == "success" %>
  var idComment = <%= @id_comment %>;

  $('#picture_comment[data-comment="'+idComment+'"]').parent().html("<%= escape_javascript(render 'picture_comment', picture: @picture) %>");
<% else %>
  alert('error!');
<% end %>

$("#user-modal").modal("hide");
